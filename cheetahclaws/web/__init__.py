"""CheetahClaws Web Terminal — xterm.js + WebSocket + basic auth.

Usage:
    cheetahclaws --web                  # start on port 8080
    cheetahclaws --web --port 9090      # custom port
    cheetahclaws --web --no-auth        # disable password (local only)
"""
