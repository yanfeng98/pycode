"""PyCode Web Terminal — xterm.js + WebSocket + basic auth.

Usage:
    pycode --web                  # start on port 8080
    pycode --web --port 9090      # custom port
    pycode --web --no-auth        # disable password (local only)
"""
